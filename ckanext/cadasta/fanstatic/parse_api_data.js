function getActivityData () {

    var activityData = $('#activity-data').data().obj;

    var numFeatures = activityData.features.length;

    var html = '';

    for (var i = 0; i < numFeatures; i++) {

    };

    return activityData;
}
